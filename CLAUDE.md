# upmu-pahak 작업 규칙

이 저장소는 **공개**입니다. 그리고 두 곳에서 커밋이 들어옵니다 — 화영님 PC의
Claude Code, 그리고 클라우드 세션(데일리 브리프 루틴 등).

## 1. 작업을 시작하면 먼저 `git pull`

```bash
git pull
```

같이 쓰는 [aside-skills](https://github.com/shway81-droid/aside-skills)에서
2026-09-18에 사고가 났습니다. 로컬의 하루 전 파일을 올려 **익명화가 통째로
덮이고** 동료 담당자 실명이 공개 저장소의 최신 파일로 되돌아갔습니다.
파일을 열기 전에 `git pull` 한 번이 그걸 막습니다.

## 2. 예시에는 실제 값을 쓰지 않는다 — `○○` 표기

`references/config-format.md`의 예시가 기준입니다.

| 쓰지 않는다 | 쓴다 |
|---|---|
| 실제 학교 이름 | `○○초등학교`, `○○초` |
| 담당자 이름 (`drafters`, `exclude.drafters`) | `김○○`, `박○○` |

업무명·`pattern`·`flow`처럼 **학교마다 비슷한 행정 절차**는 예시로 두어도
됩니다. 사람 이름과 학교 이름만 안 됩니다 — 그 둘이 붙으면 누가 무슨 일을
맡는지가 드러나고, 그건 본인 정보가 아니라 제3자 정보입니다.

## 3. 실제 산출물은 저장소에 커밋하지 않는다

이 스킬이 만드는 것은 **사용자의 공문 폴더 안**에 생깁니다.

- `_작업/docs.json`·`titles.txt`·`tasks.json`·`overrides.json`·`notes.json`
- `<학교약칭>_업무파악.html`

여기에는 실제 학교명·담당자 이름·공문 제목·본문 요지가 들어 있습니다.
실수로 저장소 안에서 스킬을 돌렸을 때를 대비해 `.gitignore`로 막아 두었지만,
**커밋 목록에 이 이름들이 보이면 멈추고 빼십시오.**

## 4. 커밋 전에 검사

```bash
python scripts/check_anonymized.py
```

통과하면 종료 코드 0, 익명화가 안 된 곳이 있으면 1과 함께 파일·줄번호를
알려 줍니다. 같은 검사가 **push 될 때 GitHub Actions로도 돌아갑니다**
(`.github/workflows/privacy-check.yml`).

검사기에는 **실제 이름을 적지 않습니다.** 이름 목록을 넣으면 그 목록 자체가
노출이기 때문에, "○ 가 없는 이름 자리"를 패턴으로 잡습니다.

## 5. 이미 올라간 민감정보를 지울 때는 PR을 만들지 않는다

**직접 커밋합니다.** PR로 처리하면 지운 내용이 PR 화면의 diff(빨간 줄)로
**영구 보존되고, 공개 저장소에서는 그게 그대로 보입니다.** 머지된 PR은
GitHub에서 지울 수 없습니다.

순서: 직접 커밋 → 필요하면 그 뒤에 이력 정리.

## 화면을 고칠 때

`assets/template.html`과 스크립트는 **정규식으로 일괄 수정하지 않습니다.**
CSS를 지우려던 패턴이 JS 줄까지 지워 스크립트가 깨진 적이 있습니다. 정확한
문자열을 한 곳씩(개수 1 확인) 바꿉니다. 자세한 것은 `SKILL.md`의 「고칠 때
주의」를 보십시오.
