---
name: upmu-pahak
description: >-
  학교의 1년치 기안 공문 폴더(K-에듀파인에서 내려받은 "(학교명-문서번호 (본문)) 제목.pdf" 와 첨부들)와
  업무분장표를 가지고, 업무 담당자가 자기 업무를 한눈에 파악하는 단일 HTML "업무 길잡이"
  (<학교약칭>_업무파악.html)를 만드는 전 과정. 본문 PDF에서 결재일·수신처·근거 공문·붙임을 뽑고,
  첨부를 연결하고, 업무분장 기준으로 분류하고, 공문을 읽어 업무별 "1년 흐름"을 쓰고, 부서 탭·업무명 칩·
  월 카드·검색이 되는 화면을 생성·검증한다. 사용자가 "업무파악 폴더 분석해서 html 만들어줘",
  "작년 공문으로 업무 인수인계 자료 만들어줘", "새로 맡은 업무 파악하게 공문 정리해줘",
  "우리 학교 공문 폴더로 업무 길잡이 만들어줘", "업무분장별로 공문 정리" 같은 요청을 하거나,
  폴더에 "(○○학교-1234 (본문)) …pdf" 형식 파일이 수십~수천 개 있으면 반드시 이 스킬을 쓴다.
  공문 한 건 작성·기안문 초안, 개별 한글 문서 편집은 이 스킬의 범위가 아니다.
metadata:
  category: education
  locale: ko-KR
---

# 업무파악 — 공문 폴더로 "업무 길잡이" 만들기

새 학년도에 업무를 넘겨받은 교사가 가장 알고 싶은 것은 하나다.
**"내 업무는 1년 동안 언제 무엇을 했고, 그때 쓴 문서는 어디 있나?"**
작년 기안 공문을 그대로 따라 하는 것이 학교에서 일을 배우는 실제 방식이므로, 이 스킬은 공문 폴더를
**부서 → (업무명) → 월** 순서로 다시 엮어 클릭 한 번에 작년 문서가 열리는 HTML 한 장을 만든다.

## 결과물이 지켜야 할 방향 (사용자와 여러 번 고쳐서 확정한 것)

목적은 *분석 보고*가 아니라 *담당자가 한눈에 자기 업무를 파악*하는 것이다. 그래서 화면에는 다음만 있다.

- 상단: 부서 탭 + 검색. 그게 전부다.
- 부서 페이지: 업무명 칩(`전체`가 맨 왼쪽) → 월 카드(`전체`가 맨 왼쪽, 누르면 그 달만, 부서를 바꿔도 유지) → 월별 문서 목록(월 제목을 누르면 접힘).
- 업무명 칩을 누르면 그 업무 문서만 남고 위에 **1년 흐름**(단계 칩)이 나온다.
- 문서 목록은 **표(문서대장) 모양**이다: `결재일 | 제목 | 구분 | 근거 공문 | 문서` 다섯 열, 고정 머리글, 줄무늬, 월 구분 행.
  "가시성이 첫째"라는 요청으로 시안 5종(큰 글씨·표·타임라인·색 카드·왼쪽 메뉴)을 비교해 사용자가 표를 골랐다 — 표는 열이 맞아 훑기가 가장 빠르고 교사에게 익숙한 대장 모양이다.
- 제목을 누르면 본문 요지가 펼쳐진다. 핵심 문서는 노란 줄. `📄 본문` `📎 첨부`는 맨 오른쪽 열.

넣지 않기로 한 것: 담당자 이름·건수가 붙은 탭, 통계 줄(연 n건·주요 발신처), "알아둘 점" 상자, 업무 고르기 첫 화면,
전체 월별 보기 탭. 담당자는 해마다 바뀌고, 숫자와 해설은 "파악"을 늦춘다. 새 요소를 제안하고 싶어지면
"이게 담당자가 자기 업무를 더 빨리 찾게 해 주는가?"로 걸러라. 화면은 `assets/template.html`에 고정돼 있으니
사용자가 요청하지 않는 한 고치지 않는다.

## 준비물 (없으면 멈추고 요청한다)

1. **공문 폴더** — 파일명이 `(학교명-문서번호 (본문|첨부)) 제목.확장자`. 하위 폴더 구조는 자유(부서/월, 부서만, 폴더 없음 모두 가능).
2. **업무분장표** — 필수. 업무명은 학교마다 쓰는 말이 달라서, 지어낸 분류로는 교사가 자기 업무를 못 찾는다.
   폴더 안에서 "업무분장"이 들어간 hwpx/hwp/pdf/xlsx를 찾아보고(공문 첨부에 들어 있는 경우가 많다), 없으면
   사용자에게 폴더에 넣어 달라고 요청하고 기다린다. 업무분장표 없이 진행하지 않는다.
3. Python 3 + PyMuPDF(`fitz`, 없으면 `pypdf`). Windows에서는 `PYTHONUTF8=1`을 켜고 실행한다(한글 출력이 깨져 멀쩡한 결과를 실패로 오해하기 쉽다).

원본 공문 폴더는 읽기만 한다. 만드는 것은 `<폴더>/_작업/`(중간 산출물)과 `<폴더>/<학교약칭>_업무파악.html` 뿐이다.

## 흐름

```
① 훑어보기 → ② 업무분장표 읽기 → ③ 추출 → ④ tasks.json 작성 → ⑤ 분류·다듬기 → ⑥ 공문 읽고 1년 흐름 쓰기 → ⑦ 생성 → ⑧ 검증·눈으로 확인 → ⑨ 안내
```
추출을 tasks.json보다 먼저 하는 이유: 분류 규칙(pattern)은 실제 제목 목록을 보고 써야 하고, 담당자 이름도 추출된 기안자 표기와 맞춰야 한다.

스크립트는 모두 `scripts/`에 있고 `--root "<공문 폴더>"` 하나만 받는다.

### ① 훑어보기
```bash
PYTHONUTF8=1 python scripts/extract.py --root "<폴더>" --scan-only
```
본문/첨부 수, 최상위 폴더, 규칙 밖 파일을 보고 사용자에게 한두 줄로 알린다. 본문이 0건이면 파일명 규칙이 다른 것이니
실제 파일명 몇 개를 보여 주고 어떻게 내려받은 것인지 묻는다.

### ② 업무분장표 읽기
hwpx는 `python scripts/read_hwpx.py "<파일>"`로 표를 텍스트로 본다. 구형 hwp는 `hwp` 스킬을 쓰거나 hwpx/PDF로 다시 저장해 달라고 한다.
읽어 낼 것: **부서(계) – 담당자 – 업무 내용**. 행정실(행정실장·주무관·시설·통학차량·조리 등)은 **항상 제외**한다 —
이 자료는 교원(및 늘봄·돌봄·방과후 담당) 업무 파악용이다.

### ③ 추출
```bash
PYTHONUTF8=1 python scripts/extract.py --root "<폴더>"
```
`_작업/docs.json`과 **`_작업/titles.txt`**(폴더별 제목·기안자 목록)가 생긴다. 출력의 "폴더×기안자" 표가 업무분장표와 맞는지 본다.
기안자에 사람 이름이 아닌 말이 섞였거나 결재일 누락이 많으면 그 PDF 원문을 몇 개 열어 결재란 모양을 확인한다.
"글자가 추출되지 않는 PDF"는 스캔본이라 제목·첨부만으로 분류된다 — 사용자에게 건수를 알린다.
본문의 휴대전화 번호·주민번호는 추출 단계에서 자동으로 가려진다(`010-****-****`).

### ④ `_작업/tasks.json` 작성
`titles.txt`를 옆에 두고 쓴다. 형식과 예시는 `references/config-format.md`. 핵심만:

- **group = 화면의 탭.** 한 사람이 통째로 맡는 단위로 잡는다(보통 공문 폴더의 최상위 폴더 = 부서). 작은 학교는 부서 하나를
  한 사람이 다 맡으므로 "내 업무 = 부서"다. 폴더가 부서로 나뉘어 있지 않으면 `drafters`(기안자 이름)로 묶는다.
- **task = 업무명 칩.** 업무분장표의 "계/업무" 이름을 그대로 쓴다. 교사가 자기 업무분장표에서 본 그 말이어야 찾는다.
  부서당 4~10개가 적당하다. 업무분장표 항목이 너무 잘면 같은 담당자·같은 성격끼리 묶는다.
- **pattern** = 공문 *제목*에 거는 정규식. 위에서부터 첫 매칭이 이기므로 **구체적인 업무를 위에, 포괄적인 업무(교무기획 등)를 맨 아래에** 둔다.
  그러면 칩 순서가 업무분장표와 달라지는데, 그때는 task에 `"order": 1, 2, …`를 주어 **표시 순서만** 업무분장표대로 맞춘다.
- **칩 하나에 문서가 45건을 넘으면** `업무명 · 하위 주제`로 나눈다(예: `보건·체육 · 아침 간편식`, `체험 · 해외문화탐방`). 업무분장표에 계가 하나뿐인
  부서(돌봄·방과후 등)도 같은 방식으로 3개 안팎으로 나눈다. 반대로 문서가 몇 건뿐이어도 업무분장표에 있는 업무면 칩으로 둔다.
- **업무분장표의 문언을 우선한다.** 분장표에 없는 일(예: 교육활동 보호)은 그 문서를 기안한 부서에서 성격이 가장 가까운 업무에 넣는다.
- `exclude.drafters`에 행정실 직원 이름을, `exclude.folders`에 행정실 폴더명을 넣는다. 폴더에 행정실 문서가 애초에 없으면 제외 0건이 정상이다.
- 담임으로서 한 일·본인 수업공개처럼 "그 부서에서만 기타로 보낼 것"은 group에 `"etc_pattern"`을 둔다(전역 `etc_pattern`은 의회·감사 요구자료용).

### ⑤ 분류하고 다듬기 — 여기서 품질이 갈린다
```bash
PYTHONUTF8=1 python scripts/classify.py --root "<폴더>" --list 전체
```
**목록을 끝까지 읽는다.** 업무마다 "이 제목이 정말 이 업무인가"를 보고 고친다.

- 규칙으로 해결되면 `pattern`을 고치고, 한두 건짜리 예외는 `_작업/overrides.json`(`{"2025-2158": "task_id"}`)에 넣는다.
- `*` 표시는 어떤 규칙에도 안 걸려 "기타"로 간 문서다. 기타가 10%를 넘으면 규칙이 부족한 것이다.
  기타에 남겨도 되는 것: 의회·국회·감사원 요구자료, 담임으로서 한 일(가족체험학습 신청 등), 본인 수업공개.
- **한국어 키워드는 낱말 경계가 없다.** `의회`는 `협의회`에, `인사`는 다른 낱말 속에도 걸린다. 짧은 키워드를 넣었으면
  그 패턴에 걸린 제목을 반드시 눈으로 확인한다. 순서로 해결하거나(더 구체적인 업무를 위로) 더 긴 표현을 쓴다.
- 같은 사업의 문서(신청 → 성립전 예산 → 운영계획 → 물품 구입 → 결과보고)가 여러 업무로 흩어지지 않게 한다.
  교사는 사업 단위로 기억한다. 다른 부서 폴더에 들어가 있는 문서도 `overrides.json`에 **다른 부서의 task id**를 주면 그 부서 탭으로 옮겨진다.
- classify.py가 끝에 알려 주는 것을 본다: 본문에만 의회·감사 표현이 있는 문서, 45건을 넘는 업무, 0건인 업무.

### ⑥ 공문을 읽고 1년 흐름 쓰기 → `_작업/notes.json`
```bash
PYTHONUTF8=1 python scripts/dump.py --root "<폴더>"
```
`_작업/dump/<부서>.txt`를 **전부** 읽는다(수백 건이어도 본문은 대개 두세 문장이다). 제목만 보고 쓰면 "언제 무엇을 받아 무엇을 했는지"가
빠진다 — 근거 공문 날짜, 위원회 → 학운위 → 집행 같은 절차 순서는 본문에만 있다.
분량은 문서 240건에 6만 토큰쯤이라 한 번에 안 읽힌다. 파일을 `#####` 업무 단위로 나눠 읽고, **한 업무를 읽을 때마다 바로 그 업무의 flow를 써 둔다**(다 읽고 몰아 쓰면 앞의 것을 잊는다).

업무마다 `flow` 한 줄과 `key`(핵심 문서 id 3~6개)를 쓴다. 형식은 `references/config-format.md`.

- `flow`는 `3월 계획 수립·담당자 현황 제출 → 4월 실태조사 안내 → … → 1월 결과보고`처럼 **시기로 시작하는 단계를 `→`로** 잇는다.
  화면이 `→`에서 끊어 단계 칩으로 보여 준다. 한 단계 안의 세부 순서는 괄호 안에 쓴다: `10월 가산점 선정(인사자문위 → 선정위 1·2차 → 명단 제출)`.
- 시기는 `3월`, `5~6월`, `6·9·11월`, `10월~2월`, `매월`, `매월 말`, `수시`, `연중`, `1학기`, `여름방학 중`, `학년 말`로 쓴다(이 형태만 굵게 강조된다).
  괄호 밖 `→` 뒤에 시기 없이 단계를 쓰면 시기 없는 칩이 생긴다 — build_html.py가 "시기 없이 시작하는 단계"를 알려 주니, 앞 단계의 괄호 안으로 옮기거나 시기를 붙인다.
- 사실만 쓴다. 공문에 없는 절차를 일반론으로 채우지 않는다. 문서가 많은 업무는 8~14단계, 문서가 몇 건뿐인 업무는 2~4단계면 된다.
- `key`에는 그 업무의 연간 계획서, 반복되는 절차의 첫 문서, 최종 결과보고처럼 "이것부터 열어 보면 되는" 문서를 고른다.

### ⑦ 생성
```bash
PYTHONUTF8=1 python scripts/build_html.py --root "<폴더>"
```

### ⑧ 검증하고 눈으로 확인
```bash
PYTHONUTF8=1 python scripts/verify.py --root "<폴더>"
```
모두 OK여야 한다(링크 대상 파일 100% 존재, 첨부 전수 연결, 문서가 있는 모든 업무에 1년 흐름, 스크립트 문법).
그다음 **브라우저에서 직접 눌러 본다**: 부서 탭 전환, 업무명 칩, 월 카드, 검색, `📄 본문` 링크. 내장 브라우저가 `file://`을 못 열면
그 폴더에서 `python -m http.server 8765 --bind 127.0.0.1`을 백그라운드로 띄워 `http://127.0.0.1:8765/<퍼센트 인코딩한 파일명>`으로 확인한다
(한글 파일명은 퍼센트 인코딩이 필요하다). 확인이 끝나면 서버를 끄고, 확인용으로 만든 설정 파일(`.claude/launch.json` 등)이 있으면 지운다.

### ⑨ 사용자에게 안내할 것
- 결과 파일 위치와 건수(문서·첨부·업무 수), 제외한 것(행정실 n건), 기타로 남긴 것의 성격.
- **폴더째 옮겨야** 링크가 유지된다(HTML만 따로 옮기면 끊긴다).
- **교내 공유 전용.** 본문 요지에 학생·강사 실명이 들어 있을 수 있으니(전화번호·주민번호만 자동으로 가린다) 학교 홈페이지 등 외부에 올리지 않도록 알린다.
- PDF는 브라우저에서 바로 열리고 **hwp·hwpx·xlsx는 다운로드**된다 — 브라우저가 못 여는 형식이라 그렇다.
  한 번 받은 뒤 다운로드 목록에서 "이 유형의 파일은 항상 열기"를 켜면 한글에서 바로 열린다.
- 분류가 어색한 곳을 알려 주면 `overrides.json`만 고쳐 다시 생성하면 된다.

## 고칠 때 주의

- `assets/template.html`이나 스크립트 안의 코드를 **정규식으로 일괄 수정하지 않는다.** CSS를 지우려던 `\.group…` 패턴이
  JS의 `D.groups` 줄까지 지워 스크립트가 깨진 적이 있다. 정확한 문자열을 한 곳씩(개수 1 확인) 바꾼다.
- 편집기 되돌리기(rewind)는 직접 고친 파일만 되돌린다. HTML은 스크립트가 만든 산출물이라 되돌린 뒤 `build_html.py`를 다시 실행해야 한다.
- 문서번호는 매년 1월에 1번으로 돌아간다. 그래서 문서 id는 `결재연도-번호`(예: `2025-2142`, `2026-66`)이고, `overrides.json`과 `notes.json`의 `key`도 이 id를 쓴다.
